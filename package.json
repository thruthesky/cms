{
  "name": "cms",
  "version": "1.0.0",
  "description": "CMS for community projects",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "./node_modules/.bin/cypress run --env server=wordpress.philgo.com --config integrationFolder=tests/cypress,testFiles=**/*.spec.js && npm run push:live",
    "push:live": "git add . && git commit -m 'push:live'",
    "cypress:run": "./node_modules/.bin/cypress run --config integrationFolder=src,testFiles=**/*.spec.js",
    "cypress:open": "./node_modules/.bin/cypress open --config integrationFolder=src,testFiles=**/*.spec.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thruthesky/cms.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/thruthesky/cms/issues"
  },
  "homepage": "https://github.com/thruthesky/cms#readme",
  "devDependencies": {
    "@types/faker": "^4.1.12",
    "chokidar": "^3.4.2",
    "cypress": "^5.0.0",
    "faker": "^5.0.0",
    "sass": "^1.26.10",
    "socket.io": "^2.3.0"
  }
}
